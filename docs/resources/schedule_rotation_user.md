---
page_title: "Resource rootly_schedule_rotation_user - terraform-provider-rootly"
subcategory:
description: |-
    This resource is deprecated. Please use the schedule_rotation_members attribute within the rootly_schedule_rotation schedule_rotation.md resource instead. This resource will be removed in the next major version.
---

# Resource (rootly_schedule_rotation_user)

This resource is deprecated. Please use the `schedule_rotation_members` attribute within the [`rootly_schedule_rotation`](schedule_rotation.md) resource instead. This resource will be removed in the next major version.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `user_id` (Number) Schedule rotation user

### Optional

- `position` (Number) Position of the user inside rotation
- `schedule_rotation_id` (String)

### Read-Only

- `id` (String) The ID of this resource.

## Import

rootly_schedule_rotation_user can be imported using the [`import` command](https://developer.hashicorp.com/terraform/cli/commands/import).

```sh
terraform import rootly_schedule_rotation_user.primary a816421c-6ceb-481a-87c4-585e47451f24
```

Or using an [`import` block](https://developer.hashicorp.com/terraform/language/import).

```terraform
import {
  to = rootly_schedule_rotation_user.primary
  id = "a816421c-6ceb-481a-87c4-585e47451f24"
}
```

Locate the resource id in the web app, or retrieve it by listing resources through the API if it's not visible in the web app.

HCL can be generated from the import block using the `-generate-config-out` flag.

```sh
terraform plan -generate-config-out=generated.tf
```

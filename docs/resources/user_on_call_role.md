---
page_title: "Resource rootly_user_on_call_role - terraform-provider-rootly"
subcategory:
description: |-
    Manages the on-call role assigned to a Rootly user. The resource id is the user id. Destroying it stops managing the assignment but leaves the user's on-call role unchanged.
---

# Resource (rootly_user_on_call_role)

Manages the on-call role assigned to a Rootly user. The resource id is the user id. Destroying it stops managing the assignment but leaves the user's on-call role unchanged.

## Example Usage

```shell
data "rootly_user" "john" {
  email = "john@acme.com"
}

resource "rootly_user_on_call_role" "john" {
  user_id         = data.rootly_user.john.id
  on_call_role_id = "54725583-b47a-4397-aa50-c6b1db3c1da0"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `on_call_role_id` (String) The id of the on-call role to assign.
- `user_id` (String) The id of the user to assign the on-call role to.

### Read-Only

- `id` (String) The ID of this resource.

## Import

rootly_user_on_call_role can be imported using the [`import` command](https://developer.hashicorp.com/terraform/cli/commands/import).

```sh
terraform import rootly_user_on_call_role.primary a816421c-6ceb-481a-87c4-585e47451f24
```

Or using an [`import` block](https://developer.hashicorp.com/terraform/language/import).

```terraform
import {
  to = rootly_user_on_call_role.primary
  id = "a816421c-6ceb-481a-87c4-585e47451f24"
}
```

Locate the resource id in the web app, or retrieve it by listing resources through the API if it's not visible in the web app.

HCL can be generated from the import block using the `-generate-config-out` flag.

```sh
terraform plan -generate-config-out=generated.tf
```

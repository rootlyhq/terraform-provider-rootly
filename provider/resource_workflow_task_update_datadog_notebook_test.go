package provider

// Skipped: API rejects empty 'kind' but field is optional with no default.

import "testing"

func TestAccResourceWorkflowTaskUpdateDatadogNotebook(t *testing.T) {
	t.Skip("API rejects empty 'kind' but field is optional with no swagger default")
}

package provider

// Skipped: API requires max_tokens > 0 but swagger doesn't mark it required.

import "testing"

func TestAccResourceWorkflowTaskCreateMistralChatCompletion(t *testing.T) {
	t.Skip("API requires max_tokens > 0 but field is not required in schema")
}

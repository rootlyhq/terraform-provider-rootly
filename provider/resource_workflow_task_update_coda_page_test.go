package provider

// Skipped: API requires 'doc' but swagger doesn't mark it required.

import "testing"

func TestAccResourceWorkflowTaskUpdateCodaPage(t *testing.T) {
	t.Skip("API requires 'doc' but not in swagger required")
}

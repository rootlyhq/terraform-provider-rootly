import type { oas30 } from "openapi3-ts";
import type { ResourceConfig } from "./schema";
import { camelize, singularize } from "inflection";
import { assertSchemaObject } from "./types";
import { generateModel } from "./generate-common";

export function generateResource({
  doc,
  config,
}: {
  doc: oas30.OpenAPIObject;
  config: ResourceConfig;
}) {
  console.log(`Generating resource: ${config.name}`);

  let schema = doc.components?.schemas?.[config.name];
  if (!schema) {
    throw new Error(`Cannot find schema: ${config.name} in doc`);
  }
  assertSchemaObject(schema);

  const name = `${camelize(config.name)}Resource`;
  const modelName = `${camelize(config.name)}ResourceModel`;

  return `// Code generated by providergen. DO NOT EDIT.
package provider

import (
	"context"

	"github.com/hashicorp/terraform-plugin-framework/resource"
	"github.com/hashicorp/terraform-plugin-framework/resource/schema"
)

var _ resource.Resource = &${name}{}
var _ resource.ResourceWithConfigure = &${name}{}
var _ resource.ResourceWithImportState = &EscalationPathResource{}

func New${name}() resource.Resource {
	return &${name}{}
}

type ${name} struct {
	baseResource
}

func (r *${name}) Metadata(ctx context.Context, req resource.MetadataRequest, resp *resource.MetadataResponse) {
	resp.TypeName = req.ProviderTypeName + "_${config.name}"
}

func (r *${name}) Schema(ctx context.Context, req resource.SchemaRequest, resp *resource.SchemaResponse) {
	resp.Schema = schema.Schema{
		MarkdownDescription: ${JSON.stringify(config.description ?? "")},
		Attributes: map[string]schema.Attribute{
			// Attributes will be generated here
		},
	}
}

func (r *${name}) Create(ctx context.Context, req resource.CreateRequest, resp *resource.CreateResponse) {
	// Create logic will be generated here
}

func (r *${name}) Read(ctx context.Context, req resource.ReadRequest, resp *resource.ReadResponse) {
	// Read logic will be generated here
}

func (r *${name}) Update(ctx context.Context, req resource.UpdateRequest, resp *resource.UpdateResponse) {
	// Update logic will be generated here
}

func (r *${name}) Delete(ctx context.Context, req resource.DeleteRequest, resp *resource.DeleteResponse) {
	// Delete logic will be generated here
}

func (r *${name}) ImportState(ctx context.Context, req resource.ImportStateRequest, resp *resource.ImportStateResponse) {
	// Import logic will be generated here
}

${generateModel({
	config,
	schema,
	baseName: camelize(singularize(config.name)),
	name: modelName,
	isTopLevel: true,
})}
`;
`;
}
